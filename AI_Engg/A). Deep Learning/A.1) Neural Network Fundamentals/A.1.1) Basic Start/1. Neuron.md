A neuron is the basic information processing unit in a neural network.
There are five basic elements in a neuron. A neuron is denoted as **k**.

```mermaid

graph LR
    %% Input nodes mapping to weights
    x1(("x<sub>k1</sub>")) --> w1(("w<sub>k1</sub>"))
    x2(("x<sub>k2</sub>")) --> w2(("w<sub>k2</sub>"))
    x3(("x<sub>k3</sub>")) --> w3(("w<sub>k3</sub>"))
    dots("...")
    xm(("x<sub>km</sub>")) --> wm(("w<sub>km</sub>"))

    %% Weights mapping to Linear Combiner
    w1 --> sum(("Σ"))
    w2 --> sum
    w3 --> sum
    wm --> sum

    %% Forward propagation path
    sum -- "u<sub>k</sub>" --> bias(("b<sub>k</sub>"))
    bias -- "v<sub>k</sub>" --> act(("φ(v<sub>k</sub>)"))
    act --> out(("y<sub>k</sub>"))

    %% Bottom descriptions and pointers based on g.webp
    Synapses["Synapses"] -.-> xm
    Parameters["Parameters"] -.-> wm
    Combiner["Linear<br>Combiner"] -.-> sum
    BiasLabel["Bias"] -.-> bias
    ActLabel["Activation<br>Function"] -.-> act
    OutLabel["Output"] -.-> out

    %% Styling for labels and structural dots to remove borders
    style dots fill:transparent, stroke:transparent
    style Synapses fill:transparent, stroke:transparent
    style Parameters fill:transparent, stroke:transparent
    style Combiner fill:transparent, stroke:transparent
    style BiasLabel fill:transparent, stroke:transparent
    style ActLabel fill:transparent, stroke:transparent
    style OutLabel fill:transparent, stroke:transparent
```

#Synapses

A **synapse** is basically an input signal to your neuron. A synapse is also known as a **connecting link**. Every neuron in a neural network expects a set of synapses.

A synapse is denoted as **$x_{kj}$** , where _k_ represents the neuron and _j_ represents the index of the synapse. The total number of synapses accepted by a neuron is denoted by **m**.